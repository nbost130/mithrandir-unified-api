/**
 * Transcription Palantir API Types
 *
 * This file contains TypeScript types generated from the Transcription Palantir OpenAPI specification.
 *
 * To regenerate these types:
 * 1. Ensure Transcription Palantir API is running (default: http://localhost:3001)
 * 2. Run: npm run generate:types
 *
 * For custom API URL:
 * TRANSCRIPTION_API_URL=http://palantir.tailnet:3001 npm run generate:types
 *
 * See README.md for complete documentation.
 */

// Placeholder - run `npm run generate:types` to generate actual types
export type paths = Record<string, any>;
export type components = Record<string, any>;
